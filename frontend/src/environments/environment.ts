// Ambiente padrão (desenvolvimento). Substituído em build de produção
// via fileReplacements no angular.json por environment.prod.ts.
export const environment = {
  production: false,
  apiBaseUrl: 'http://127.0.0.1:8000',
};
