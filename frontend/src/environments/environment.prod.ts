// Ambiente de produção. Ajustar apiBaseUrl para o endpoint público da API
// (ex.: domínio do Application Load Balancer / API Gateway na AWS).
export const environment = {
  production: true,
  apiBaseUrl: 'https://api.condoguard.example.com',
};
